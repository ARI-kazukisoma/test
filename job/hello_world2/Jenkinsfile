@Library('auto-deploy') _

pipeline {
  agent any
  stages {
    stage("shared library import test") {
      steps {
        node("master") {
          script {
            def slack = new org.slack.Util()
            // slack.notifyMessage('#hooktest', "bbbbbb")
            slack.echoTest("aaaaaaa")
          }
        }
        node("master") {
          script {
            def slack = new org.slack.Util()
            // slack.notifyMessage('#hooktest', "aaaaa")
          }
        }
      }
    }
  }
}